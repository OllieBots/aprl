name: Deploy to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel Deploy Hook
        run: |
          curl -sS -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}" -w "\nHTTP status: %{http_code}\n"
